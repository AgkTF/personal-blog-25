---
type Props = {
  title: string;
  link: string;
};

const { title, link } = Astro.props;
---

<h2
  class="font-bold font-headline text-headline text-lg lg:text-xl hover:text-link"
>
  <a href={link}>{title}</a>
</h2>
